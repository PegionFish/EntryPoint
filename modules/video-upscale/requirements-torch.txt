# === 基础依赖（自动并入，M2 单文件语义：每后端文件必须自足）===
# adapter 基础依赖（所有后端共享；视频处理走系统 ffmpeg）
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6

# === 后端专属 ===
# torch 路线依赖（cuda / rocm 后端）——M2 requirements_by_backend 消费落地前需手动安装
#
# 注意（如实记录）：
# - CUDA wheel 需按平台索引安装，例如：
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# - ROCm wheel（RX 7900 XTX gfx1100）：
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/rocm6.4
# - basicsr 与新版 torchvision 存在 torchvision.transforms.functional_tensor 移除问题，
#   若导入报错请将 torchvision 钉在 <0.17 或对 basicsr 打 upstream 补丁；
#   realesrgan 0.3.0 的依赖声明较旧，建议先装 torch 再装其余项。
torch>=2.4
torchvision
basicsr>=1.4.2
realesrgan>=0.3.0
opencv-python-headless>=4.8
numpy>=1.24
