# torch cu130 wheel 索引（RTX 5090 D / Blackwell sm_120 需 cu128+ 构建）。
# 默认 PyPI 索引的 torch 变体不含 sm_120 支持；cu130 索引经
# constraints 的 "==2.11.0" 约束收敛（PEP 440：2.11.0+cu130 满足 ==2.11.0）。
# 机制依据：config/constraints.txt 头部【wheel 索引提示】a) requirements 内联。
--extra-index-url https://download.pytorch.org/whl/cu130

fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
deepfilternet>=0.5.6
soundfile>=0.12.0
numpy>=1.24.0
# torch 与 config/constraints.txt 锁定的版本一致（同 pin 恒不冲突；
# constraints 存在时以 -c 强制收敛，二者取值相同）。升级须与 constraints
# 一起修改（选型依据见 constraints.txt【torch 全家桶】）。
torch==2.11.0
# torchaudio 与 torch 同族配对（C++ 扩展硬配对，跨版本导入即断）；
# 锁 2.11.0 使 uv 在 cu130 索引命中 2.11.0+cu130 Windows wheel。
torchaudio==2.11.0
# scipy：adapter.py 的 fallback 频谱减法路径 + 重采样使用。
# 保底 1.14.0：与 torch cu130 栈的运行环境对齐
# （构建于 numpy 1.x 的旧 scipy 轮子在 numpy 2.x 下会报 dtype size 错误）。
scipy>=1.14.0
