# rembg — openvino 后端依赖（经 module.toml [runtime].requirements_by_backend 消费，
# HETERO_DIST_PLAN M2；venv 随 M3 按 <module>--<backend> 分建）。
#
# 与基础 requirements.txt 的 onnxruntime 兼容策略：
# 1) 基础栈经 rembg[cpu] extra 安装官方 CPU 版 onnxruntime，区间为
#    >=1.23.2,<2.0.0（rembg 2.0.81 的 extra 约束，实测 METADATA）；
# 2) onnxruntime-openvino 是 Intel 发布的 ORT 整合 wheel（内置 OpenVINO EP +
#    CPU EP），提供同一个 `onnxruntime` 包命名空间——与官方 onnxruntime
#    互斥、不可共存于同一 venv。因此本文件刻意省略 rembg[cpu] 的 cpu extra，
#    改由本 wheel 提供运行时；
# 3) 版本对齐：沿用与基础栈完全相同的区间 >=1.23.2,<2.0.0，解析得最新
#    ORT-OV 1.24.1（对应 ORT 1.24.1 + OpenVINO 2025.4.1），cp310–cp313 wheel，
#    覆盖 runtime.python_version >=3.10,<3.13；
# 4) 设备选择：adapter 读 OPENVINO_DEVICE 注入 provider option 键
#    device_type（GPU.0 / NPU.0 / CPU / AUTO:...），见 ort_ep.py docstring。
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
Pillow>=10.0.0
pymatting>=1.1.15
onnxruntime-openvino>=1.23.2,<2.0.0
rembg>=2.0.50
