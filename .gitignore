# Rust
/target/
target-*/
**/*.rs.bk
Cargo.lock

# Node
node_modules/

# IDE
.idea/
.vscode/
*.swp
*.swo
.qwen/
.qoder/

# 会话/导出产物（不入仓库）
HF
qwen-code-export-*.md

# 会话交接记录（不入仓库）
session-*.md

# OS
.DS_Store
Thumbs.db
desktop.ini

# Runtime（自动生成：venvs/、cuda-libs/、.uv-cache/ 等，不入 git）
workspace/
logs/
*.log
runtime/

# Fallback binaries (not in git/release)
modules/test-ffmpeg/

# Build artifacts & model files (user-managed)
dist/
models/

# Config overrides (user-specific)
config/gpu_assignments.toml

# Python 缓存
__pycache__/
*.pyc

# 解除忽略（追加，覆盖上方 target-* / Cargo.lock 规则）：
# - Cargo.lock：二进制 workspace 应提交，供 PKGBUILD 的 cargo build --locked 使用
!Cargo.lock

# 第三方 AI 工具本地目录
.codewhale/
